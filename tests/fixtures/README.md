# Test fixtures

`sample_a.jpg`, `sample_b.jpg` — two views of the same scene (Sacré-Cœur),
sourced from the LightGlue repo (`thirdparty/LightGlue/assets/sacre_coeur{1,2}.jpg`).
License: Apache 2.0.

Used by `tests/smoke/` and any contract tests that need a real image pair.
